# AI & Team Conventions for KOSPI Prediction Project

이 문서는 팀원들이 사용하는 모든 AI 어시스턴트(Gemini, Cursor, Copilot, Cline 등)와 팀원들이 공통으로 준수해야 할 프로젝트 규칙 및 가이드라인입니다.

## 1. 프로젝트 개요 (Project Overview)
- **목표:** 코스피(KOSPI) 지수의 방향성(상승/하락) 예측
- **주요 방법론:** 머신러닝 (Machine Learning) 및 딥러닝 기반 시계열 분류/회귀

## 2. AI 어시스턴트 주의사항 (AI Assistant Guidelines)

모든 AI는 코드를 작성하거나 구조를 제안할 때 다음 사항을 엄격히 준수해야 합니다:

### 2.1. 사전 코드 작성 금지 (No Preemptive Coding)
- 사용자가 명시적으로 코드 작성을 요청하기 전까지는 임의로 전체 코드를 작성하거나 완성하지 마세요.
- 질문이나 아이디어 단계에서는 로직 설계, 데이터 흐름, 구조에 대한 논의에 집중하세요.

### 2.2. 폴더 구조 엄수 및 파일 저장 규칙 (Strict Directory & File Saving Rules)
**🚨 [AI 필수 지침] AI 어시스턴트는 새로운 파이썬 코드를 작성하거나 데이터를 저장할 때, 반드시 아래 명시된 폴더 구조를 확인하고 역할에 맞는 정확한 경로에 파일을 생성 및 저장해야 합니다. 임의의 위치에 파일을 만들지 마세요.**

*   **`src/data/`**: 외부에서 데이터를 가져오는 코드 (예: API 연동, 크롤링 스크립트 등)
*   **`src/preprocessing/`**: 가져온 원본 데이터를 정제하고 다듬는 코드 (결측치 처리, 스케일링 등)
*   **`src/features/`**: 새로운 지표나 파생 변수(이동평균, RSI 등)를 생성하는 피처 엔지니어링 코드
*   **`src/models/`**: 모델(XGBoost, LSTM 등)을 정의하고 학습(Train)시키는 핵심 코드
*   **`src/evaluation/`**: 학습된 모델의 성능을 평가하고 시계열 교차검증을 수행하는 코드
*   **`src/strategies/`**: AI 예측 결과를 바탕으로 매매 시그널(규칙)을 생성하는 코드
*   **`src/backtest/`**: 매매 전략을 과거 데이터에 적용하여 수익률을 시뮬레이션하는 코드
*   **`data/`**: 코드를 통해 수집된 실제 원본 및 가공 데이터 파일 (CSV, JSON 등 - *Git 업로드 제외 권장*)
*   **`outputs/`**: 학습이 완료된 모델 파일(`.pkl`, `.h5`) 및 최종 분석 차트 이미지
*   **`experiments/`**: 하이퍼파라미터 튜닝 결과, 실험 로그 등 텍스트 결과물
*   **`notebooks/`**: 데이터 탐색(EDA) 및 실험용 주피터 노트북(`.ipynb`) 파일
*   **`docs/`**: 프로젝트 기획서, 아키텍처 다이어그램 등 관련 문서

### 2.3. 실험 기록 및 재현성 (Reproducibility)
- 모든 모델 학습 및 평가 결과는 `experiments/` 또는 `outputs/`에 기록되는 구조를 염두에 두고 로직을 설계하세요.
- 랜덤 시드(Random Seed) 고정은 필수입니다.

### 2.4. 작업 진행 상황 확인 (Task Management via TODO.md)
**🚨 [AI 필수 지침] 프로젝트의 진행 상황은 최상단에 있는 `TODO.md` 파일에 기록되어 있습니다.**
- AI 어시스턴트는 사용자의 작업 요청을 받으면 **가장 먼저 `TODO.md` 파일을 읽어 프로젝트가 어느 단계까지 진행되었는지 파악해야** 합니다.
- 작업을 완료한 후에는 사용자의 승인 하에 `TODO.md`의 체크리스트를 업데이트(`[ ]` -> `[x]`) 하도록 제안해야 합니다.

---
**AI 프롬프트 지침:** AI 어시스턴트는 이 파일을 컨텍스트로 읽어들인 후, 코스피 방향성 예측이라는 도메인 특성 및 **위의 폴더 구조와 TODO.md 확인 규칙을 완벽하게 숙지한 상태에서** 답변과 코드를 생성해야 합니다.